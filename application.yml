spring:
  application:
    name: CONFIG-Default
  cloud:
    config:
      server:
        bootstrap: true
        git:
          uri: https://github.com/hardik10553/microservice-tutorial-config
          clone-on-start: true

